<%
  budget = local_assigns.fetch(:budget)
  date = local_assigns.fetch(:date)
  presented_budget = BudgetPresenter.new(budget, date)
%>

<%= turbo_stream.replace dom_id(budget, date) do %>
  <%= render "budgets/budget", budget: presented_budget, date: date %>
<% end %>
