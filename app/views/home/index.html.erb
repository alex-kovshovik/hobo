<section class="section">
  <div class="container">
    <h1 class="title">
      Welcome to HOBO!
    </h1>
    <p class="subtitle">
      Highly Opinionated <strong>Budget</strong> Organizer!
    </p>

    <% if Current.user %>
      <%= link_to "Expenses", budgets_path %><br/>
      <%= link_to "Logout", session_path, data: { turbo_method: :delete } %>
    <% else %>
      <%= link_to "Login", session_path %>
    <% end %>
  </div>
</section>

